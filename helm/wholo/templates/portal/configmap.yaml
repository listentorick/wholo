apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wholo.fullname" . }}-portal
  labels:
    app: {{ include "wholo.fullname" . }}-portal
data:
  NEXT_PUBLIC_API_URL: {{ .Values.portal.apiUrl | quote }}
